const path=require('path'),{pathToFileURL}=require('url');const {chromium}=require('C:/Users/alchera/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage();await p.goto(pathToFileURL(path.join(process.cwd(),'index.html')).href);await p.locator('body > .section-elevator > .arcade-open').click();await p.waitForTimeout(2000);const state=()=>p.locator('.arcade-background-video').evaluate(v=>({time:v.currentTime,duration:v.duration,paused:v.paused,ended:v.ended,loop:v.loop,ready:v.readyState,error:v.error?.message,reduced:matchMedia('(prefers-reduced-motion: reduce)').matches}));console.log('initial',await state());await p.locator('.arcade-background-video').evaluate(v=>{v.currentTime=v.duration-.5;});await p.waitForTimeout(2500);console.log('after end',await state());await p.emulateMedia({reducedMotion:'reduce'});await p.waitForTimeout(300);console.log('reduced motion',await state());}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
