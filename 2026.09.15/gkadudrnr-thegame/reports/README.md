# Historical simulation reports

The JSON files were moved from the temporary output directory to preserve the evidence behind the balance reports. They predate some current rules and are not current balance verification. New simulations still write their generated results under `output/`.
